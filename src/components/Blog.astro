---
const { header, images, excerpt, date, link } = Astro.props;
---

<article
  class="bg-white shadow-lg rounded-lg overflow-hidden hover:shadow-xl transition-shadow duration-300"
>
  <img src={images} alt={titles} class="w-full h-64 object-cover" />

  <div class="p-6">
    <h2 class="text-2xl font-serif text-gray-800 mb-2">{header}</h2>
    <p class="text-sm text-gray-500 mb-4">{date}</p>
    <p class="text-gray-700 text-base leading-relaxed mb-4">{excerpt}</p>
    <a
      href={link}
      class="inline-block text-blue-600 hover:text-blue-800 font-medium"
    >
      Read more →
    </a>
  </div>
</article>
