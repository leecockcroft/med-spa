---
const { children } = Astro.props;
import "aos/dist/aos.css";
import SplideInit from "../scripts/aos-init.js";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta title="Massage Art Estepona" />
    <meta name="viewport" content="width=device-width" />
    <title>Massage Art Estepona</title>
    <script type="module" src="/src/scripts/aos-init.js"></script>
  </head>
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    integrity="sha512-...your-integrity-key..."
    crossorigin="anonymous"
    referrerpolicy="no-referrer"
  />

  <link
    href="https://fonts.googleapis.com/css2?family=Tenor+Sans&display=swap"
    rel="stylesheet"
  />

  <link
    href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
    rel="stylesheet"
  />
  <script type="module" src="/src/scripts/showPanel.js"></script><script
    src="https://unpkg.com/taos@latest/dist/taos.js"></script>
</html>

<body
  class="bg-white text-gray-800 scroll-pt-[80px] max-w-screen-lg mx-auto"
  style="background-color: rgb(246, 245, 241);"
>
  <slot />

  <!-- <div
    id="cookie-banner"
    class="fixed bottom-0 left-0 right-0 bg-gray-800 text-white p-4 text-center z-50"
  >
    We use cookies to improve your experience. By using our site, you agree to
    our cookie policy.
    <button
      onclick="acceptCookies()"
      class="ml-4 bg-green-500 px-3 py-1 rounded">Accept</button
    >
  </div>

  <div
    id="cookie-banner"
    style="position: fixed; bottom: 20px; left: 20px; background: #fff; border: 1px solid #ccc; padding: 15px; box-shadow: 0 2px 8px rgba(0,0,0,0.2); z-index: 9999;"
  >
    <p>
      We use cookies to improve your experience and analyse website traffic. Do
      you accept?
    </p>
    <button id="accept-cookies" style="margin-right: 10px;">Accept</button>
  </div> -->

  <!-- <script>
    window.dataLayer = window.dataLayer || [];

    // Check if already accepted
    if (localStorage.getItem("cookiesAccepted") === "true") {
      dataLayer.push({ event: "consent_accepted" });
      const banner = document.getElementById("cookie-banner");
      if (banner) banner.style.display = "none";
    }

    // Handle accept click
    document
      .getElementById("accept-cookies")
      .addEventListener("click", function () {
        localStorage.setItem("cookiesAccepted", "true");
        document.getElementById("cookie-banner").style.display = "none";
        dataLayer.push({ event: "consent_accepted" });
      });
  </script> -->

  <script>
    import AOS from "aos";
    import "aos/dist/aos.css";

    AOS.init();
  </script>
</body>
